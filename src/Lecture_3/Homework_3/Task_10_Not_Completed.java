package Lecture_3.Homework_3;

public class Task_10_Not_Completed {

    public static void main(String[] args) {

        int [] array = {4, 16, 7, 8, 12, 23, 56, 64};
        int start = 0;
        int end = array.length -1;
        int length = array.length;
        boolean isZigZag = false;

        if (array.length % 2 == 0) {
            for (int i = start; i < length; i++) {
                if (i % 3 == 0) {
                    // check that first/previous element is smaller than next, and then check that second is bigger than
                    // next
                }
            }
        }
    }
}
